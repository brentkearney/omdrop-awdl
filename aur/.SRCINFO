pkgbase = brcmfmac-awdl-dkms
	pkgdesc = AWDL (AirDrop link layer) support for the BCM4387: DKMS module and root helpers
	pkgver = 0.1.0
	pkgrel = 1
	url = https://github.com/brentkearney/omdrop-awdl
	install = brcmfmac-awdl-dkms.install
	arch = aarch64
	license = GPL-2.0-only
	depends = dkms
	depends = bash
	depends = python
	depends = iproute2
	depends = iputils
	depends = kmod
	depends = polkit
	depends = procps-ng
	depends = util-linux
	depends = systemd
	optdepends = linux-asahi-headers: build against the Asahi kernel
	optdepends = networkmanager: keeps awdl0 unmanaged and settles the Wi-Fi MAC before awdl0 is derived from it
	optdepends = opendrop: the AirDrop receiver the omdrop plugin runs on top of this (AUR; there is no python-opendrop)
	optdepends = bluez: the opt-in BLE Continuity advert
	optdepends = python-dbus: the opt-in BLE Continuity advert
	optdepends = python-gobject: the opt-in BLE Continuity advert
	source = brcmfmac-awdl-dkms-0.1.0.tar.gz::https://github.com/brentkearney/omdrop-awdl/archive/v0.1.0.tar.gz
	sha256sums = SKIP

pkgname = brcmfmac-awdl-dkms
